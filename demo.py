from dup_fmt.formatter_replace import Naming

if __name__ == "__main__":
    nm = Naming(
        {
            "strings": "data engineer",
        }
    )
    nm2 = Naming(
        {
            "strings": "data engineer",
        }
    )
